import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DonateLayoutComponent } from './donate-layout.component';

describe('DonateLayoutComponent', () => {
  // let component: DonateLayoutComponent;
  // let fixture: ComponentFixture<DonateLayoutComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [DonateLayoutComponent],
  //   }).compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DonateLayoutComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
