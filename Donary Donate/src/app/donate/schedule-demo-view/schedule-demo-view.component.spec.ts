import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ScheduleDemoViewComponent } from './schedule-demo-view.component';

describe('ScheduleDemoViewComponent', () => {
  // let component: ScheduleDemoViewComponent;
  // let fixture: ComponentFixture<ScheduleDemoViewComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [ScheduleDemoViewComponent],
  //   }).compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ScheduleDemoViewComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
