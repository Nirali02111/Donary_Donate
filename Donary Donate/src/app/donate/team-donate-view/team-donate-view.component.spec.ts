import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeamDonateViewComponent } from './team-donate-view.component';

describe('TeamDonateViewComponent', () => {
  // let component: TeamDonateViewComponent;
  // let fixture: ComponentFixture<TeamDonateViewComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [TeamDonateViewComponent],
  //   }).compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TeamDonateViewComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
