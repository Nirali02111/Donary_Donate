import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreatePayComponent } from './create-pay.component';

describe('CreatePayComponent', () => {
  // let component: CreatePayComponent;
  // let fixture: ComponentFixture<CreatePayComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CreatePayComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CreatePayComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
