import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DonateDonorListItemComponent } from './donate-donor-list-item.component';

describe('DonateDonorListItemComponent', () => {
  // let component: DonateDonorListItemComponent;
  // let fixture: ComponentFixture<DonateDonorListItemComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [DonateDonorListItemComponent],
  //   }).compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DonateDonorListItemComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
