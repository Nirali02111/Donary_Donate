import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VoucherProcessComponent } from './voucher-process.component';

describe('VoucherProcessComponent', () => {
  // let component: VoucherProcessComponent;
  // let fixture: ComponentFixture<VoucherProcessComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [VoucherProcessComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(VoucherProcessComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
