import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VoucherSuccessComponent } from './voucher-success.component';

describe('VoucherSuccessComponent', () => {
  // let component: VoucherSuccessComponent;
  // let fixture: ComponentFixture<VoucherSuccessComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [VoucherSuccessComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(VoucherSuccessComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
