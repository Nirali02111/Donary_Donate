import { TestBed } from '@angular/core/testing';

import { CharityTokenService } from './charity-token.service';

describe('CharityTokenService', () => {
  // let service: CharityTokenService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(CharityTokenService);
  // });

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
