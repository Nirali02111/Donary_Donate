import { TestBed } from '@angular/core/testing';

import { SmartPayAPIService } from './smart-pay-api.service';

describe('SmartPayAPIService', () => {
  // let service: SmartPayAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(SmartPayAPIService);
  // });

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
