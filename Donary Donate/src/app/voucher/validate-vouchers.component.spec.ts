import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidateVouchersComponent } from './validate-vouchers.component';

describe('ValidateVouchersComponent', () => {
  // let component: ValidateVouchersComponent;
  // let fixture: ComponentFixture<ValidateVouchersComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [ValidateVouchersComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(ValidateVouchersComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
